
images:
  - type: invalid_type  # Invalid type
    repo: test
    tag: latest
nodes:
  - name: node1
    image: ubuntu:20.04
    interfaces:
      - name: eth0
        mode: invalid_mode  # Invalid mode
        ip: "not_a_list"    # Should be a list
